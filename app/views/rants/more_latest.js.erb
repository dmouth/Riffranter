<% if @rants.count != 0 %>
  $("#rant-container.latest").append("<%= escape_javascript(@html)%>");

  <% for rant in @rants %>
    bindRatingControl($('.rating-control[data-rant-id="<%= rant.id %>"]'));
  <% end %>           
<% else %>
  alert("No more rants!");
<% end %>